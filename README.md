En este apartado se valida la autenticacion del usuario enviando un OTP al gmail fuente, para ello es necesario tener una app pass en la cuenta desde donde se enviara la OTP. Para correr el codigo, se debe crear un proyecto nodejs e instalar las siguientes dependencias con npm:  express, express-session, nodemailer y mongoose(para hacer pruebas en base de datos)

